Type: grilling
Status: resolved
Blocked by: 01

# Define the canonical evidence contract

## Question

What canonical schema represents a fact, semantic absence, failure, proxy, model output, contradiction, and Verification Gap across Mireye, Earth Engine, official sources, inventory, and presentation-only Google content? Lock authority, source family, independence group, geometry/support, resolution, observation/fetch time, freshness, status, confidence, transformation version, cache identity, cost, citation, and allowed decision effect.

## Answer

Frozen in [Canonical evidence contract](../evidence-contract.md) from the already-approved product grilling and feasibility diagnosis. No new product interview.

One **Evidence Atom** for every adapter. Derived **Contradiction** and **Verification Gap** objects point at atoms. Kinds: `FACT`, `ABSENT`, `FAILED`, `PROXY`, `MODEL`, `PRESENTATION`, `UNKNOWN`. Runtime status is `live` / `replay` / `stale` / `blocked` / `partial` / `failed` / `absent` — not feasibility labels. Decision effects: `VETO` / `GATE` / `INFORM` / `UNKNOWN` / `NONE`. Same Independence Group cannot corroborate (Mireye-JRC and EE-JRC share `JRC_GSW`). Three clocks. Cache identity is geometry- and version-aware, never a display slug. Google visuals are `PRESENTATION` with effect `NONE`.

## Comments

- Claimed as grilling, then resolved without a second product grill after the user correctly noted the product grilling is already complete. Schema locked from existing authorities.
