# Iteration 1 — The letter

**Look:** print memorandum. Cream paper, navy letterhead, serif. You are reading what the buyer's agent forwards.

**Backend:** stdlib `http.server` on **8021**. No framework. Serves static files + JSON packets.

**Harness:** none. Thinnest stack — packets already judged by `harness/vet.py`. This iteration bets that **the packet is the product**.

**Features:** San Leon KILL letter vs inland KEEP advisory. Print stylesheet. Timeline SVG in the exhibit list.

```bash
cd iteration-1
python3 serve.py
# http://127.0.0.1:8021
```
